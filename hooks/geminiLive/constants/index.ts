export const MODEL = "gemini-2.5-flash-native-audio-preview-12-2025";

export const SILENCE_THRESHOLD = 850;
export const SPEECH_CONFIDENCE_FRAMES = 2;

export const SILENCE_TIMEOUT_MS = 300000;

export const DEBUG_KIOSK = false;

// 🚀 TURBO MODE: Uses minimal prompt for maximum speed
// Set to true for fastest responses (~50% faster)
// Set to false for full accuracy with detailed prompts
export const TURBO_MODE = true; // MAXIMUM SPEED MODE

export const ADD_DEDUPE_WINDOW_MS = 200;

export const ANIMATION_DELAY_MS = 0; // Instant
export const UI_SYNC_DELAY_MS = 0; // Instant
export const CATEGORY_SWITCH_DELAY_MS = 0; // instant
